<section class="hero">
  <div class="container">
    <div class="row">
      <div class="col-lg-3">
        <div class="hero__categories">
          <div class="hero__categories__all">
            <i class="fa fa-bars"></i>
            <span>Danh mục</span>
          </div>
            <ul>
            {{#each category}}
              <li><a
                  href="/shop-grid/{{this.idCategory}}"
                >{{this.name}}</a></li>
            {{/each}}
          </ul>
          </div>
      </div>

      <div class="col-lg-9">
        <div class="hero__search">
          <div class="hero__search__form">
            <form action="/shop-grid/search" method="GET">
              <input
                name="name"
                type="text"
                placeholder="Bạn muốn mua gì"
                style="color: black;"
                value="{{name}}"
              />
              <button type="submit" class="site-btn" >Tìm kiếm</button>
            </form>
          </div>

          <div class="hero__search__cart">
            <div class="hero__search__cart__icon">
              <a href="/shoping-cart"> <i class="fas fa-shopping-cart" ></i></a>
            </div>
          </div>

          <div class="hero__search__phone">
            <div class="hero__search__phone__icon">
              <a href="tel:19001000"><i class="fa fa-phone"></i></a>
            </div>

            <div class="hero__search__phone__text">
              <h5>1900.1000</h5>
              <span>Hỗ trợ nhanh chóng</span>
            </div>
          </div>

        </div>
        <div class="hero__item set-bg" data-setbg="./img/hero/banner.png"> {{!-- cần tìm 1 ảnh banner ở đây. --}}
          <div class="hero__text">
            <span>Nội thất sang trọng</span>
            <h2>Giá cả rẻ <br />100% mới mẻ</h2>
            <p>GIAO HÀNG TOÀN QUỐC</p>
            <a href="/shop-grid" class="primary-btn">Mua ngay</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>